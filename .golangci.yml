run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - gosimple
  disable:
    - exhaustive
    - wrapcheck

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
